- Put a footer-textfile into the announce report
- Announces templates
- Announces to *.PKT

- Document tokens (add to tparser & dumpcfg):
    filefixkillreports
    filefixkillrequests
    semadir
    badfilesdir
    autofilecreate
    autofilecreatedefaults
    savetick
    execonfile
    origininannounce
    busyFileDir
    -sendorig
    ConvertShortNames
    ConvertLongNames
    forwardFileRequestFile
    ForwardFilePriority
    forwardFileRequests
    filefixhelp

- New tokens for files.bbs handle:
    filesBBSname	(default: files.bbs)
    descriptionsSubDir	(no default)
    
    If descriptionsSubDir set, every file has it's own description in this
    dir, i.e. "file.ext" has description in ".desc/file.ext.desc"

- Change delNotReceivedTIC value from boolean to numberical, wait N days before
remove tic (0 = immediately)
